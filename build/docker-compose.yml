services:
  runner:
    build:
      context: ../
      dockerfile: ./build/Dockerfile
    container_name: go-db-etl
    volumes:
      - "../src.test.json:/app/src.test.json"
    env_file:
      - ../.env
